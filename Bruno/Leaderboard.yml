info:
  name: Leaderboard
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/leaderboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
