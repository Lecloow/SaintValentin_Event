info:
  name: RevealAllHints
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/me/hints/1/reveal-all"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
