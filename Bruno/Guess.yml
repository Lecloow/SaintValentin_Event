info:
  name: Guess
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/me/guess"
  body:
    type: json
    data: |-
      {
        "day": 1,
        "hintNumber": 1,
        "guessedUserId": 3
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
