info:
  name: Matches
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/me/matches"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
