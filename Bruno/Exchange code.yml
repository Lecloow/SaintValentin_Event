info:
  name: Exchange code
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/me/revealcode/1/exchange"
  body:
    type: json
    data: |-
      {
        "code": "test"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
