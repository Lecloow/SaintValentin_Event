info:
  name: Candidates
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/me/candidates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
