# PostgreSQL Database Connection
# For local development, use a local PostgreSQL instance
# For Render deployment, this will be automatically set by Render
DATABASE_URL=postgresql://user:password@localhost:5432/saintvalentin

# Email configuration for sending passwords
EMAIL=your-email@example.com
PASSWORD=your-app-password
