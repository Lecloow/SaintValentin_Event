info:
  name: Reveal Match
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/me/matches/1/reveal"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
