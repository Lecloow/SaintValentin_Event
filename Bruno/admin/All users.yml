info:
  name: All users
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admin/users"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
