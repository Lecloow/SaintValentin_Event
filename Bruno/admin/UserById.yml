info:
  name: UserById
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/admin/users/51"
  body:
    type: form-urlencoded
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
