info:
  name: Hints
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/me/hints"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
