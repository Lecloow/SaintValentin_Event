info:
  name: RevealSpecificHint
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/me/hints/1/1/reveal"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
