info:
  name: RevealCode
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/me/revealcode"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
