info:
  name: CreateHints
  type: http
  seq: 21

http:
  method: POST
  url: "{{baseUrl}}/admin/createHints"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
